# Locally bundled, reproducible third-party browser assets. Keep repository
# whitespace checks focused on authored sources rather than upstream minified code.
guardian/sveltia-cms.js -whitespace
pagefind/** -whitespace
