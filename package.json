{
  "name": "rainbow-sanctuary-website",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "node --test tests/*.test.mjs",
    "validate": "npm test && python3 scripts/validate_site.py && node scripts/publish-discovery-layer.mjs && git diff --exit-code"
  },
  "dependencies": {
    "stripe": "22.4.0"
  }
}
